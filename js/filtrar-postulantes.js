// Elimino el submit handler para que el formulario use solo el action y el script del HTML.

const formFiltro = document.getElementById('form-filtro');
const resultados = document.getElementById('resultados-postulantes');

// function fetchPostulantes() {
//   // Aquí se obtendrían los postulantes filtrados del backend
//   // Mostrar en #resultados-postulantes
// } 